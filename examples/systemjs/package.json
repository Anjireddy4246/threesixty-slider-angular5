{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "rm -rf build && mkdir build && tsc -p src -w",
    "build": "rm -rf build && mkdir build && tsc -p src || true",
    "start": "http-server -c-1 ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^1.7.5"
  },
  "dependencies": {
    "http-server": "^0.8.5",
    "systemjs": "^0.19.9",
    "@angular/common": "^5.0.0",
    "@angular/compiler": "^5.0.0",
    "@angular/core": "^5.0.0",
    "@angular/http": "^5.0.0",
    "@angular/platform-browser": "^5.0.0",
    "@angular/platform-browser-dynamic": "^5.0.0",
    "rxjs": "^5.0.0",
    "zone.js": "^0.8.4",
    "es6-promise": "^3.0.2",
    "core-js": "^2.4.0",
    "reflect-metadata": "^0.1.2",
    "ts-loader": "^0.8.2"
  }
}
